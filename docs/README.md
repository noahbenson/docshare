# `docshare` documentation

* [`spec.md`](spec.md) --- the specification of record for `docshare`.

The repository `README.md` is user-facing documentation; `spec.md` is the
normative specification that the implementation is checked against.
